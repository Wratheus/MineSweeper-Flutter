name: minesweeper
description: "minesweeper flutter game."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.8.1


dependencies:
  window_size: ^0.1.0
  audioplayers: ^6.5.0
  flutter:
    sdk: flutter


  cupertino_icons: ^1.0.8
  provider: ^6.1.5
  confetti: ^0.8.0

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^6.0.0

flutter:
  assets:
    - assets/img/
    - assets/sounds/
  uses-material-design: true